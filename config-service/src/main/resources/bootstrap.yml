server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Dino-Enterprises/Microservices.git
          searchPaths: config-data
          user-name: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "game-management"
  security:
    user:
      name: root
      password: s3cr3t
